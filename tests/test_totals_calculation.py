import pytest
pytest.skip(
    "Deprecated after unified cluster-capacity report refactor; totals verified via new alignment tests.",
    allow_module_level=True,
)
import pytest

pytest.skip(
    "Deprecated after unified cluster-capacity report refactor; totals verified via new alignment tests.",
    allow_module_level=True,
)
